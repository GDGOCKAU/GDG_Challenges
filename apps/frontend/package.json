{
  "name": "@gdg/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GDG Challenges web app (Vite + React + TypeScript)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "scriptNotes": {
    "dev": "Starts the Vite development server for the React frontend.",
    "build": "Type-checks the frontend, then creates the production Vite build.",
    "preview": "Serves the already-built Vite production output locally for review.",
    "typecheck": "Checks frontend TypeScript without creating a production build.",
    "clean": "Removes frontend dist/ build output."
  },
  "dependencies": {
    "@gdg/shared": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.18.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.1",
    "@vitejs/plugin-react": "^5.0.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  }
}
