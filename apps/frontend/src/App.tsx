/**
 * Placeholder shell. Pages, routing and the GDG design system come later —
 * this file only proves the frontend base builds and runs.
 */
export function App() {
  return (
    <main>
      <h1>GDG Challenges</h1>
      <p>Frontend base is running.</p>
    </main>
  );
}
